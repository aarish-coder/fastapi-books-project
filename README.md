# fastapi-books-project
A lightweight RESTful API built with FastAPI to manage a digital book library. The system allows users to perform CRUD (Create, Read, Update, Delete) operations on book records, using a JSON-based persistent storage system. It features automatic API documentation via Swagger UI, making it easy for developers to test and integrate.
